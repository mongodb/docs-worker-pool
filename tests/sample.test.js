//const job = require('../jobTypes/githubPushJob');

describe('Test Class', () => {
    beforeAll(() => {
      
    });
  
    afterAll(() => {
      
    });

    beforeEach(() => {
      
    });
  
    afterEach(() => {
      
    });
  
    
    it('sampleTest', async() => {
      expect(1 + 1).toEqual(2);
    });

});