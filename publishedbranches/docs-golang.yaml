prefix: 'drivers/go'
version:
  published:
    - 'master (upcoming)'
    - 'v1.8 (current)'
    - 'v1.7'
  active:
    - 'master (upcoming)'
    - 'v1.8 (current)'
    - 'v1.7'
  stable: 'v1.8'
  upcoming: master
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      - 'v1.8'
      - 'v1.7'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...
