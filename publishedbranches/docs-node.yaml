prefix: 'drivers/node'
version:
  published:
    - 'master'
  active:
    - 'master'
  stable: 'master'
  upcoming: master
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...
