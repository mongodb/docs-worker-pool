prefix: 'drivers/node'
version:
  published:
    - '4.0 (upcoming)'
    - '3.6 (stable)'
  active:
    - '4.0 (upcoming)'
    - '3.6 (stable)'
  stable: 'v3.6'
  upcoming: 'v4.0'
git:
  branches:
    manual: 'v4.0'
    published:
      - 'master'
      - 'v3.6'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...
