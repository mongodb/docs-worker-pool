prefix: 'drivers/node'
version:
  published:
    - '4.x (upcoming)'
    - '4.0 (current)'
    - '3.6'
  active:
    - '4.x (upcoming)'
    - '4.0 (current)'
    - '3.6 (current)'
  stable: 'v4.0'
  upcoming: 'master'
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      - 'v4.0'
      - 'v3.6'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...
