prefix: 'charts'
version:
  published:
    - 'master'
    - '19.12'
  active:
    - 'master'
    - '19.12'
  stable: 'master'
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      - '19.12'

      # the branches/published list **must** be ordered from most to
      # least recent release.
...
