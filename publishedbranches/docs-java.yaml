prefix: 'drivers/java/sync'
version:
  published:
    - 'master (upcoming)'
    - '4.3 (current)'
  active:
    - 'master (upcoming)'
    - '4.3 (current)'
  stable: 'v4.3'
  upcoming: 'master'
git:
  branches:
    manual: 'master'
    published:
      - 'master'
      - 'v4.3'
      # the branches/published list **must** be ordered from most to
      # least recent release.
...
